import React from "react"
import {Text, Window} from "@/elements"

const Home = () => (
    <Window>
        <Text header>Welcome to YouQuest</Text>
    </Window>
)

Home.propTypes = {}

export default Home